@if (isDemoOn())
    <div class="col-12 mt-3">
        <div class="alert alert-warning" role="alert">
            {{ localize("Currently showing from generated data for DEMO Mode.") }}
        </div>
    </div>
@endif
